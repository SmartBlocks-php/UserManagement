define([
    'jquery',
    'underscore',
    'backbone'
], function ($, _, Backbone) {

    var init_login = function () {

    };


    var main = {
        init: function () {
            init_login();
        }
    };

    return main;
});